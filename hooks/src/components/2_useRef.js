import { useRef } from "react";

function UseRef() {
    const ref = useRef(null);

    /*
        js에서 DOM SELECTOR와 같이 DOM에 접근할 수 있게 하는 혹함수
        render와 상관없이 값을 유지 시키는 변수
    */

    return <div></div>;
}
export default UseRef;
